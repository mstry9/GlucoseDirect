workflows:
  ios-build:
    name: Glucose Direct Build
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      xcode: latest
    scripts:
      - name: Initialize Keychain and Signing
        script: |
          keychain initialize
          # Corrected 2026 Type: IOS_DEVELOPMENT
          app-store-connect certificates create --type IOS_DEVELOPMENT
          # Corrected 2026 Fetch Type: development
          app-store-connect fetch-signing-files "com.mstry9.GlucoseDirect" --type development --create
          keychain add-certificates
      - name: Install Pods
        script: |
          pod install
      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --project "GlucoseDirect.xcodeproj" \
            --scheme "GlucoseDirect" \
            --bundle-id "com.mstry9.GlucoseDirect"
    artifacts:
      - build/ios/ipa/*.ipa
